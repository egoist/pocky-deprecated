{
  "private": true,
  "name": "pocky",
  "scripts": {
    "lint": "xo",
    "test": "lerna bootstrap && xo && lerna exec -- npm test"
  },
  "devDependencies": {
    "eslint-config-rem": "^3.2.0",
    "lerna": "^2.0.0-rc.5",
    "xo": "^0.18.2"
  },
  "xo": {
    "extends": "rem",
    "envs": [
      "jest"
    ]
  }
}
